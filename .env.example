# Optional Supabase config for syncing (set in your hosting or Vite env)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# When running locally, copy to .env and fill in values if you provision Supabase
# DO NOT commit secrets to public repos. Use GitHub Actions secrets for CI deploys.
